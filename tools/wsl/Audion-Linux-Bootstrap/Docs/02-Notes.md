# Notes

These scripts intentionally keep all terminal output in English for encoding safety.

The scripts do not contain Windows drive paths. They are self-contained Linux bootstrap scripts.

Installed Python packages:

- google-generativeai
- python-docx
- pypdf
- pandas
- openpyxl
- pymorphy3
- tqdm
